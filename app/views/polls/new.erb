<div class='container'>
  <h3>Take a poll:</h3>
    <ul>
      <% @surveys.each do |survey| %>
        <li><a href="/surveys/<%= survey.id %>/polls/new"><%= survey.name %></a> <%= "(created #{survey.nice_date} by #{survey.owner})" %></li>
      <% end %>
    </ul>
</div>